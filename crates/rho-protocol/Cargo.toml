[package]
name = "rho-protocol"
version = "0.1.0"
edition = "2021"
description = "Language-neutral wire protocol types for bounded Rho agent runs"
license = "MIT"
repository = "https://github.com/pyrex41/rho"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

